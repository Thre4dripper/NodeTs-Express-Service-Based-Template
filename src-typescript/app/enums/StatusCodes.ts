export enum StatusCodes {
    SUCCESS = 200,
    CREATED = 201,
    UNAUTHORISED = 401,
    CONFLICT = 409,
    BAD_REQUEST = 400,
    SERVER_ERROR = 500,
    NOT_FOUND = 404,
    ALREADY_EXISTS = 403,
}
